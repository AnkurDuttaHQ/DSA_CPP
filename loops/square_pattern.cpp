#include <iostream>

int main() {
    const int rows = 4;
    const int columns = 4;

    for (int row = 0; row < rows; row++) {
        for (int column = 0; column < columns; column++) {
            std::cout << "* ";
 
       }
        std::cout << '\n';
    }
}
